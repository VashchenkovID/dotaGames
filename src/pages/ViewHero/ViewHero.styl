.container
  color #DBE4ED
  margin 24px
  background: rgba(63,81,181,0.3)
  padding 24px
  border-radius 10px
  box-shadow: 0px 8px 16px rgba(16, 0, 37, 0.15);
  height calc(100vh - 150px)

  .sections
    display flex
    gap:24px
    @media (max-width 1100px)
      flex-direction column
